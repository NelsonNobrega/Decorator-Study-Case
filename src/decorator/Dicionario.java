package decorator;

//classe que define o tipo dicionario, como um tipo de livro.

public class Dicionario extends Livro {
    public Dicionario(String titulo, String autor) {
        super(titulo, autor);
    }
}

