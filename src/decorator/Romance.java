package decorator;

//classe que define o tipo romance, como um tipo de livro.

public class Romance extends Livro {
    public Romance(String titulo, String autor) {
        super(titulo, autor);
    }
}
